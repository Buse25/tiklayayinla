'use client';

type PasswordInputProps = { value: string; onChange: (value: string) => void; showPassword: boolean; onToggle: () => void; disabled?: boolean; label?: string; placeholder?: string; autoComplete?: string };

export function PasswordInput({ value, onChange, showPassword, onToggle, disabled, label = 'Şifre', placeholder = 'Şifrenizi girin', autoComplete = 'current-password' }: PasswordInputProps) {
  return <label className="block"><span className="mb-2 block text-sm font-semibold text-slate-700">{label}</span><span className="relative block"><span className="pointer-events-none absolute inset-y-0 left-4 grid place-items-center text-slate-400 material-symbols-rounded">lock</span><input value={value} onChange={event => onChange(event.target.value)} disabled={disabled} type={showPassword ? 'text' : 'password'} autoComplete={autoComplete} placeholder={placeholder} className="w-full rounded-xl border border-slate-200 bg-slate-50 py-3.5 pl-11 pr-12 text-sm text-slate-900 outline-none transition placeholder:text-slate-400 focus:border-teal-500 focus:bg-white focus:ring-4 focus:ring-teal-100 disabled:cursor-not-allowed disabled:opacity-60" /><button type="button" onClick={onToggle} disabled={disabled} aria-label={showPassword ? 'Şifreyi gizle' : 'Şifreyi göster'} className="absolute inset-y-0 right-3 grid w-9 place-items-center rounded-lg text-slate-400 transition hover:text-teal-700 disabled:cursor-not-allowed"><span className="material-symbols-rounded text-[21px]">{showPassword ? 'visibility_off' : 'visibility'}</span></button></span></label>;
}
