import { Ionicons } from '@expo/vector-icons';
import { useState } from 'react';
import { ActivityIndicator, Pressable, StyleSheet, Text, View } from 'react-native';
import { Colors } from '@/constants/theme';
import { useAuth } from '@/context/auth-context';
export default function ProfileScreen() { const { user, signOut } = useAuth(); const [loading, setLoading] = useState(false); async function logout() { setLoading(true); await signOut(); setLoading(false); } return <View style={styles.root}><Text style={styles.eyebrow}>HESAP</Text><Text style={styles.title}>Profil</Text><View style={styles.profileCard}><View style={styles.avatar}><Text style={styles.avatarText}>{`${user?.firstName?.[0] ?? ''}${user?.lastName?.[0] ?? ''}`.toUpperCase()}</Text></View><Text style={styles.name}>{user?.firstName} {user?.lastName}</Text><Text style={styles.muted}>{user?.email}</Text><View style={styles.role}><Ionicons color={Colors.light.primary} name="shield-checkmark-outline" size={16} /><Text style={styles.roleText}>{user?.role === 'ADMIN' ? 'Sistem Yöneticisi' : 'Premium Danışman'}</Text></View></View><Pressable disabled={loading} onPress={() => void logout()} style={styles.logout}>{loading ? <ActivityIndicator color={Colors.light.error} /> : <><Ionicons color={Colors.light.error} name="log-out-outline" size={20} /><Text style={styles.logoutText}>Çıkış Yap</Text></>}</Pressable></View>; }
const styles = StyleSheet.create({ root: { backgroundColor: Colors.light.background, flex: 1, padding: 20, paddingTop: 30 }, eyebrow: { color: Colors.light.primary, fontSize: 11, fontWeight: '800', letterSpacing: 1 }, title: { color: Colors.light.text, fontSize: 27, fontWeight: '800', marginTop: 5 }, profileCard: { alignItems: 'center', backgroundColor: Colors.light.card, borderColor: Colors.light.border, borderRadius: 20, borderWidth: 1, marginTop: 26, padding: 25 }, avatar: { alignItems: 'center', backgroundColor: Colors.light.primary, borderRadius: 38, height: 76, justifyContent: 'center', width: 76 }, avatarText: { color: '#FFF', fontSize: 24, fontWeight: '800' }, name: { color: Colors.light.text, fontSize: 21, fontWeight: '800', marginTop: 14 }, muted: { color: Colors.light.muted, fontSize: 13, marginTop: 5 }, role: { alignItems: 'center', backgroundColor: Colors.light.soft, borderRadius: 15, flexDirection: 'row', gap: 6, marginTop: 16, paddingHorizontal: 12, paddingVertical: 8 }, roleText: { color: Colors.light.primary, fontSize: 12, fontWeight: '800' }, logout: { alignItems: 'center', borderColor: '#F2B8B5', borderRadius: 14, borderWidth: 1, flexDirection: 'row', gap: 8, justifyContent: 'center', marginTop: 20, minHeight: 50 }, logoutText: { color: Colors.light.error, fontSize: 14, fontWeight: '800' } });
