import { Pressable, StyleSheet, Text, View } from 'react-native';
import { Colors } from '@/constants/theme';
import { useAuth } from '@/context/auth-context';
export default function AdminProfile() { const { user, signOut } = useAuth(); return <View style={styles.root}><Text style={styles.eyebrow}>HESAP</Text><Text style={styles.title}>Profil</Text><View style={styles.card}><Text style={styles.name}>{user?.firstName} {user?.lastName}</Text><Text style={styles.muted}>{user?.email}</Text><Text style={styles.role}>{user?.role}</Text></View><Pressable onPress={() => void signOut()} style={styles.button}><Text style={styles.buttonText}>Çıkış Yap</Text></Pressable></View>; }
const styles = StyleSheet.create({ root: { flex: 1, backgroundColor: Colors.light.background, padding: 20, paddingTop: 32 }, eyebrow: { color: Colors.light.primary, fontWeight: '800' }, title: { color: Colors.light.text, fontSize: 26, fontWeight: '800', marginBottom: 20, marginTop: 8 }, card: { backgroundColor: Colors.light.card, borderColor: Colors.light.border, borderRadius: 16, borderWidth: 1, padding: 18 }, name: { color: Colors.light.text, fontSize: 18, fontWeight: '800' }, muted: { color: Colors.light.secondary, marginTop: 6 }, role: { color: Colors.light.primary, fontWeight: '800', marginTop: 12 }, button: { alignItems: 'center', backgroundColor: Colors.light.primary, borderRadius: 14, marginTop: 24, padding: 16 }, buttonText: { color: '#FFF', fontWeight: '800' } });
